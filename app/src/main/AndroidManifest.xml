<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.healthcalc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OpenScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalcScreen1" />
        <activity android:name=".CalcScreen2" />
        <activity android:name=".DatabaseScreen" />
        <activity android:name=".AddFoodDatabaseScreen" />
        <activity android:name=".DiaryScreen"></activity>
        <activity android:name=".Menu"></activity>
    </application>

</manifest>